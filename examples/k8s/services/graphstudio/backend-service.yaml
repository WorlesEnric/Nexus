apiVersion: v1
kind: Service
metadata:
  name: graphstudio-backend
  namespace: nexus
  labels:
    app: graphstudio-backend
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: graphstudio-backend
