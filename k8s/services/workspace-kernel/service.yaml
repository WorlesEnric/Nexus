apiVersion: v1
kind: Service
metadata:
  name: workspace-kernel
  namespace: nexus
  labels:
    app: workspace-kernel
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: workspace-kernel
