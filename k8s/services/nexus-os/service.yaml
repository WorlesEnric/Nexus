apiVersion: v1
kind: Service
metadata:
  name: nexus-os
  namespace: nexus
  labels:
    app: nexus-os
    component: ai-service
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: nexus-os
